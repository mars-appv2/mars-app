<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreateStaffMikrotikTable extends Migration
{
    public function up()
    {
        Schema::create('staff_mikrotik', function (Blueprint $table) {
            $table->id();
            $table->unsignedBigInteger('user_id')->index();      // staff user id
            $table->unsignedBigInteger('mikrotik_id')->index();  // rujuk tabel mikrotiks
            $table->timestamps();
            $table->unique(['user_id','mikrotik_id']);
        });
    }
    public function down()
    {
        Schema::dropIfExists('staff_mikrotik');
    }
}
